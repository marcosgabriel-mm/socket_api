{
  "name": "prismapi",
  "type": "module",
  "version": "1.0.0",
  "description": "Back-end api to communicate the esp32 with the database",
  "main": "src/server.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "lint": "eslint .",
    "prestart": "npm run build",
    "pretest": "npm run lint"
  },
  "author": "Helios",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^8.57.1",
    "nodemon": "^2.0.22"
  }
}
